apiVersion: v1
kind: Service
metadata:
  labels:
    app: dummy-deployment
  name: dummy-deployment
  namespace: konflux-dummy-deploy
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: dummy-deployment
